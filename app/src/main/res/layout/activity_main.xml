<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.codepath.codepath_network_sample.MainActivity">

    <ImageView
        android:layout_height="200dp"
        app:srcCompat="@android:color/darker_gray"
        android:layout_centerVertical="true"
        android:layout_centerHorizontal="true"
        android:id="@+id/ivSample"
        android:layout_width="300dp"/>

    <Button
        android:text="Get Image"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/ivSample"
        android:layout_centerHorizontal="true"
        android:id="@+id/btnGetImage"/>

    <Button
        android:text="Get IP"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/ivSample"
        android:layout_centerHorizontal="true"
        android:id="@+id/btnGetIp"/>

    <TextView
        android:text="0.0.0.0"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/txIP"
        android:layout_below="@+id/btnGetIp"
        android:layout_centerHorizontal="true"/>
</RelativeLayout>
